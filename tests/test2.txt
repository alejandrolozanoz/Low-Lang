programa low;
var
    int i, j, p;
    int Arreglo[];
    int Arreglo1[], OtroArreglo[10];
    float valor;

funcion int fact (int j)
var int i;
{
    j = (p - j * 2);

    si (j == 1) entonces {
        regresa (j);
    }
    sino {
        regresa (j * fact(j-1));
    }
}

funcion void inicia (int y)
var int x;
{
    x = 0;
    mientras (x < 11) hacer {
        Arreglo[x] = y * x;
        x = x+1;
    }
}

principal()
{
    lee(p);
    j=p*2;
    inicia (p*j - 5);

    desde i=0 hasta 9 hacer {
        Arreglo[i] = Arreglo[i] * fact(Arreglo[i] - p);
    }

    desde i=0 hasta 9 hacer {
        Arreglo[i] = Arreglo[i] * fact(Arreglo[i] - p);
    }

    mientras (i >= 0) hacer {
        escribe("resultado", Arreglo[i], fact(i+2) * valor);
        i = i - 1;
    }
    mientras (i < 10) hacer {
        escribe("Otros datos", OtroArreglo[i], p, i + OtroArreglo[i]);
        i = i + 1;
    }
}
